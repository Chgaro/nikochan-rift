# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
SITE_URL=http://127.0.0.1:8000

# Twitch embed
# Use the channel slug only (e.g. j4npu), NOT the full URL.
TWITCH_CHANNEL=j4npu
# Must match the domain used in the browser. For local dev usually "localhost".
TWITCH_PARENT=localhost
